{% extends 'base.twig'%}

{% block title %}Slim porject{% endblock %}

{% block body %}
	<h1>Liste des utilisateurs</h1>
	<table class="table table-dark">
	  <thead>
	    <tr>
	      <th scope="col">#</th>
	      <th scope="col">First</th>
	      <th scope="col">Handle</th>
	    </tr>
	  </thead>
	  <tbody>
	  	{% for user in users %}
	    <tr>
	      <th scope="row">{{ user.id() }}</th>
	      <td>{{ user.username() }}</td>
	      <td>{{ user.email() }}</td>
	    </tr>
	    {% endfor %}
	  </tbody>
	</table>{#
	<main class="container">
		<section class="row">
			{% for user in users %}
			<div class="col-4">
				<div class="card">
					<div class="card-body">
						<h5 class="card-title">{{ user.id() }}</h5>
						<p class="card-text">{{ user.username() }}
						{{ user.email() }}</p>
						<a href="/user/{{ user.id }}" class="card-link btn btn-outline-info">Détail</a>
					</div>
				</div>
			</div>
			{% endfor %}
		</section>
	</main>#}
{% endblock %}